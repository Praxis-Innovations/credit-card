openapi: 3.1.0
info:
  title: NorthTap Recommender API
  version: 0.1.0
  summary: Purchase-specific Canadian credit card recommendations
  description: |
    Contract between NorthTap clients (mobile / future authenticated web) and the
    recommender backend.

    ## Alignment with the monorepo

    - **Card & recommendation shapes** match `packages/core` (`CreditCard`,
      `Recommendation`, `Category`, etc.). Implementers SHOULD treat that TypeScript
      module as the in-process source of truth and keep this OpenAPI file in sync.
    - **Auth & owned cards** live in Supabase (`profiles`, `user_cards`).
      Catalog tables (`cards`, `loyalty_programs`, `merchant_brands`,
      `merchant_partnerships`) seed from `packages/core` and are served by
      `apps/api`. Only `status: verified` rows are public.
    - Ranking semantics match `recommendCards()`: sort by `centsPerDollar`
      descending, then lower `annualFee`, then name.

    ## Auth

    - All `/v1/*` routes except `GET /v1/health` require an API key via
      `X-Api-Key` or `Authorization: Bearer <api_key>` (not a Supabase JWT).
    - Keys are stored hashed in Supabase `api_keys` with a rate-limit tier.
    - Wallet ownership (`user_cards`) remains on Supabase PostgREST + RLS;
      this API does not implement `/me/cards` in v0.1.
    - Catalog admin write routes are intentionally unimplemented — crawler
      promotion into `cards` / partnerships tables is a separate workstream.

    ## Versioning

    All paths are under `/v1`. Breaking changes require a new major path prefix.
  contact:
    name: NorthTap
  license:
    name: Proprietary

servers:
  - url: https://api.northtap.app/v1
    description: Production (Vercel apps/api project)
  - url: http://localhost:8787/v1
    description: Local Next.js apps/api (`pnpm --filter api dev`)

tags:
  - name: Recommendations
    description: Rank owned cards for a specific purchase
  - name: Cards
    description: Credit card catalog (read)
  - name: Merchants
    description: Merchant brands and loyalty partnerships
  - name: Reference
    description: Enumerations and health
  - name: UserCards
    description: >
      Owned-card wallet — still via Supabase PostgREST + RLS
      (`supabase.from('user_cards')`). Not implemented on apps/api in v0.1.

paths:
  /recommendations:
    post:
      tags: [Recommendations]
      operationId: createRecommendation
      summary: Rank cards for a purchase
      description: |
        Accepts a purchase description and returns owned cards ranked by
        estimated value for that spend.

        **Engine input** (maps to `RecommendationInput` in `@northtap/core`):
        `ownedCardIds`, `category`, optional `spendToDate`, optional `valuations`.

        **Purchase fields** (`amountCad`, optional `merchant`) do not change
        ranking order (rank is per-dollar) but are used to compute
        `estimatedRewardCad` / `estimatedCentsBack` for the response and for
        future merchant→category hints.
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecommendationRequest"
            examples:
              groceryRun:
                summary: Groceries at Loblaws
                value:
                  amountCad: 87.42
                  category: groceries
                  merchant: Loblaws
                  ownedCardIds:
                    - amex-cobalt
                    - tangerine-money-back
                    - scotia-scene-momentum
              authenticatedWallet:
                summary: Use server-side owned cards (JWT required)
                value:
                  amountCad: 45
                  category: dining
                  merchant: Cactus Club
              withCaps:
                summary: Cap-aware ranking
                value:
                  amountCad: 120
                  category: groceries
                  ownedCardIds: [amex-cobalt, amex-simplycash]
                  spendToDate:
                    monthly:
                      groceries: 2500
      responses:
        "200":
          description: Ranked recommendations (best first)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecommendationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /cards:
    get:
      tags: [Cards]
      operationId: listCards
      summary: List credit cards in the catalog
      description: |
        Returns the curated Canadian card dataset. Filters are AND-combined.
        Default sort: issuer ascending, then name ascending.
      security:
        - ApiKeyAuth: []
      parameters:
        - $ref: "#/components/parameters/IssuerQuery"
        - $ref: "#/components/parameters/NetworkQuery"
        - $ref: "#/components/parameters/PointCurrencyQuery"
        - $ref: "#/components/parameters/CategoryQuery"
        - name: maxAnnualFee
          in: query
          description: Include only cards with annualFee ≤ this value (CAD)
          schema:
            type: number
            minimum: 0
        - name: q
          in: query
          description: Case-insensitive substring match on name or id
          schema:
            type: string
            maxLength: 100
        - $ref: "#/components/parameters/LimitQuery"
        - $ref: "#/components/parameters/OffsetQuery"
      responses:
        "200":
          description: Card page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"

    post:
      tags: [Cards]
      operationId: createCard
      summary: Create a catalog card
      description: |
        Admin / data-pipeline only. Creates a card with a stable slug `id`.
        Clients that only recommend should not call this.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreditCardWrite"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditCard"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"

  /cards/{cardId}:
    parameters:
      - $ref: "#/components/parameters/CardIdPath"
    get:
      tags: [Cards]
      operationId: getCard
      summary: Get a single card by id
      security:
        - ApiKeyAuth: []
      responses:
        "200":
          description: Card
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditCard"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [Cards]
      operationId: replaceCard
      summary: Replace a catalog card
      description: Full replacement. `id` in the body MUST match the path parameter.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreditCardWrite"
      responses:
        "200":
          description: Updated card
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditCard"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags: [Cards]
      operationId: patchCard
      summary: Partially update a catalog card
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreditCardPatch"
      responses:
        "200":
          description: Updated card
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditCard"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Cards]
      operationId: deleteCard
      summary: Delete a catalog card
      description: |
        Soft-delete preferred in implementation (retain id for historical
        `user_cards` rows). Hard delete MUST fail with 409 if any `user_cards`
        reference the slug, unless `force=true`.
      security:
        - BearerAuth: []
      parameters:
        - name: force
          in: query
          schema:
            type: boolean
            default: false
      responses:
        "204":
          description: Deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /categories:
    get:
      tags: [Reference]
      operationId: listCategories
      summary: List spend categories
      security:
        - ApiKeyAuth: []
      responses:
        "200":
          description: Categories with display labels
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CategoryListResponse"

  /issuers:
    get:
      tags: [Reference]
      operationId: listIssuers
      summary: List known issuers
      security:
        - ApiKeyAuth: []
      responses:
        "200":
          description: Issuer names
          content:
            application/json:
              schema:
                type: object
                required: [issuers]
                properties:
                  issuers:
                    type: array
                    items:
                      $ref: "#/components/schemas/Issuer"

  /point-currencies:
    get:
      tags: [Reference]
      operationId: listPointCurrencies
      summary: List reward currencies
      security:
        - ApiKeyAuth: []
      responses:
        "200":
          description: Point currencies
          content:
            application/json:
              schema:
                type: object
                required: [pointCurrencies]
                properties:
                  pointCurrencies:
                    type: array
                    items:
                      $ref: "#/components/schemas/PointCurrency"

  /valuations:
    get:
      tags: [Reference]
      operationId: getDefaultValuations
      summary: Default cents-CAD-per-point valuations
      description: |
        Mirrors `DEFAULT_POINT_VALUATIONS` in `@northtap/core`. Clients may
        override per-request via `RecommendationRequest.valuations`.
      security:
        - ApiKeyAuth: []
      responses:
        "200":
          description: Default valuations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PointValuationsResponse"


  /health:
    get:
      tags: [Reference]
      operationId: getHealth
      summary: Liveness probe
      security: []
      responses:
        "200":
          description: Service is up
          content:
            application/json:
              schema:
                type: object
                required: [status, service]
                properties:
                  status:
                    type: string
                    const: ok
                  service:
                    type: string
                  version:
                    type: string
                  time:
                    type: string
                    format: date-time

  /loyalty-programs:
    get:
      tags: [Merchants]
      operationId: listLoyaltyPrograms
      summary: List loyalty programs
      security:
        - ApiKeyAuth: []
      responses:
        "200":
          description: Loyalty programs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoyaltyProgramListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /loyalty-programs/{programId}:
    parameters:
      - name: programId
        in: path
        required: true
        schema:
          type: string
    get:
      tags: [Merchants]
      operationId: getLoyaltyProgram
      summary: Get a loyalty program by id
      security:
        - ApiKeyAuth: []
      responses:
        "200":
          description: Loyalty program
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoyaltyProgram"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /merchant-brands:
    get:
      tags: [Merchants]
      operationId: listMerchantBrands
      summary: List merchant brands
      security:
        - ApiKeyAuth: []
      parameters:
        - name: category
          in: query
          schema:
            $ref: "#/components/schemas/Category"
        - name: q
          in: query
          description: Case-insensitive substring match on name or id
          schema:
            type: string
            maxLength: 100
        - $ref: "#/components/parameters/LimitQuery"
        - $ref: "#/components/parameters/OffsetQuery"
      responses:
        "200":
          description: Brand page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantBrandListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /merchant-brands/{brandId}:
    parameters:
      - name: brandId
        in: path
        required: true
        schema:
          type: string
    get:
      tags: [Merchants]
      operationId: getMerchantBrand
      summary: Get a merchant brand by id
      security:
        - ApiKeyAuth: []
      responses:
        "200":
          description: Brand
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantBrand"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /merchant-brands/{brandId}/partnerships:
    parameters:
      - name: brandId
        in: path
        required: true
        schema:
          type: string
    get:
      tags: [Merchants]
      operationId: listPartnershipsForBrand
      summary: List partnerships for a brand
      security:
        - ApiKeyAuth: []
      responses:
        "200":
          description: Partnerships for the brand
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartnershipListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /partnerships:
    get:
      tags: [Merchants]
      operationId: listPartnerships
      summary: List merchant partnerships
      security:
        - ApiKeyAuth: []
      parameters:
        - name: cardId
          in: query
          schema:
            $ref: "#/components/schemas/CardId"
        - name: brandId
          in: query
          schema:
            type: string
        - name: loyaltyProgramId
          in: query
          schema:
            type: string
        - name: category
          in: query
          schema:
            $ref: "#/components/schemas/Category"
        - name: status
          in: query
          description: Public API only returns verified; other values yield an empty page
          schema:
            type: string
            enum: [pending, verified, stale, rejected]
        - $ref: "#/components/parameters/LimitQuery"
        - $ref: "#/components/parameters/OffsetQuery"
      responses:
        "200":
          description: Partnership page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartnershipListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /partnerships/{partnershipId}:
    parameters:
      - name: partnershipId
        in: path
        required: true
        schema:
          type: string
    get:
      tags: [Merchants]
      operationId: getPartnership
      summary: Get a partnership by id
      security:
        - ApiKeyAuth: []
      responses:
        "200":
          description: Partnership
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantPartnership"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /me/cards:
    get:
      tags: [UserCards]
      operationId: listMyCards
      summary: List the authenticated user's owned card slugs
      description: |
        Equivalent to Supabase:
        `from('user_cards').select('id, card_id, added_at').order('added_at')`.
      security:
        - BearerAuth: []
      responses:
        "200":
          description: Owned cards
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserCardListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [UserCards]
      operationId: addMyCard
      summary: Add a card to the user's wallet
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [cardId]
              properties:
                cardId:
                  $ref: "#/components/schemas/CardId"
      responses:
        "201":
          description: Added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserCard"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Unknown catalog cardId
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          $ref: "#/components/responses/Conflict"

  /me/cards/{cardId}:
    parameters:
      - $ref: "#/components/parameters/CardIdPath"
    delete:
      tags: [UserCards]
      operationId: removeMyCard
      summary: Remove a card from the user's wallet
      security:
        - BearerAuth: []
      responses:
        "204":
          description: Removed
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-Api-Key
      description: |
        NorthTap API key. Alternatively send `Authorization: Bearer <api_key>`.
        Keys are SHA-256 hashed in `public.api_keys`. Rate limits are per key
        (internal tier 600/min; standard 60/min).
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: >
        Reserved for future wallet JWT flows. v0.1 recommendations require an
        API key and explicit `ownedCardIds` in the body.

  parameters:
    CardIdPath:
      name: cardId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/CardId"
    IssuerQuery:
      name: issuer
      in: query
      schema:
        $ref: "#/components/schemas/Issuer"
    NetworkQuery:
      name: network
      in: query
      schema:
        $ref: "#/components/schemas/Network"
    PointCurrencyQuery:
      name: pointCurrency
      in: query
      schema:
        $ref: "#/components/schemas/PointCurrency"
    CategoryQuery:
      name: category
      in: query
      description: Only cards that define an earn rate for this category (or `other`)
      schema:
        $ref: "#/components/schemas/Category"
    LimitQuery:
      name: limit
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 200
        default: 100
    OffsetQuery:
      name: offset
      in: query
      schema:
        type: integer
        minimum: 0
        default: 0

  responses:
    BadRequest:
      description: Malformed request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Unauthorized:
      description: Missing or invalid bearer token
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Forbidden:
      description: Authenticated but not allowed
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Conflict:
      description: Conflict (duplicate id / still referenced)
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    UnprocessableEntity:
      description: Semantically invalid (e.g. empty owned set with no auth wallet)
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

  schemas:
    CardId:
      type: string
      pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
      minLength: 2
      maxLength: 64
      description: Stable catalog slug (matches `user_cards.card_id`)
      examples:
        - amex-cobalt
        - tangerine-money-back

    Category:
      type: string
      description: Normalized spend category (`packages/core` CATEGORIES)
      enum:
        - groceries
        - dining
        - gas
        - transit
        - drugstore
        - recurring_bills
        - travel
        - foreign_currency
        - entertainment
        - other

    Issuer:
      type: string
      description: Card issuer (`packages/core` ISSUERS). Extensible — new issuers MAY appear before the enum is updated.
      enum:
        - American Express
        - TD
        - CIBC
        - Scotiabank
        - RBC
        - BMO
        - Rogers
        - Tangerine
        - PC Financial
        - Simplii
        - Neo
        - Canadian Tire
        - National Bank
        - Desjardins
        - HSBC
        - MBNA

    PointCurrency:
      type: string
      description: Reward currency (`packages/core` POINT_CURRENCIES)
      enum:
        - Aeroplan
        - Scene+
        - TD Rewards
        - RBC Avion
        - CIBC Aventura
        - Amex MR
        - BMO Rewards
        - WestJet
        - AIR MILES
        - Marriott Bonvoy
        - cashback
        - PC Optimum
        - Rogers
        - Triangle
        - Moi

    Network:
      type: string
      enum: [Visa, Mastercard, Amex]

    RewardCategory:
      type: object
      required: [category, earnRate]
      additionalProperties: false
      properties:
        category:
          $ref: "#/components/schemas/Category"
        earnRate:
          type: number
          exclusiveMinimum: 0
          description: >
            Points (or cashback percentage points) earned per dollar spent.
            For `pointCurrency: cashback`, earnRate is the cash-back percent
            (e.g. 2 = 2%).
        capMonthly:
          type: number
          exclusiveMinimum: 0
          description: Optional monthly spend cap in CAD for this bonus rate
        capAnnual:
          type: number
          exclusiveMinimum: 0
          description: Optional annual spend cap in CAD for this bonus rate

    WelcomeOffer:
      type: object
      required: [summary]
      additionalProperties: false
      properties:
        summary:
          type: string
          minLength: 1
          maxLength: 280
          description: Human-readable signup / welcome bonus summary
        estimatedValueCad:
          type: number
          minimum: 0
          description: Rough CAD value when the offer is completed

    CreditCard:
      type: object
      required:
        - id
        - issuer
        - name
        - annualFee
        - pointCurrency
        - rewardCategories
        - lastVerified
      additionalProperties: false
      description: Catalog card — same shape as `CreditCard` in `@northtap/core`
      properties:
        id:
          $ref: "#/components/schemas/CardId"
        issuer:
          $ref: "#/components/schemas/Issuer"
        name:
          type: string
          minLength: 1
          maxLength: 120
          examples: [Cobalt, "Money-Back"]
        annualFee:
          type: number
          minimum: 0
          description: Annual fee in CAD
        pointCurrency:
          $ref: "#/components/schemas/PointCurrency"
        rewardCategories:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/RewardCategory"
        lastVerified:
          type: string
          format: date
          description: ISO date when rates were last spot-checked
        welcomeOffer:
          $ref: "#/components/schemas/WelcomeOffer"
        network:
          $ref: "#/components/schemas/Network"
        tier:
          type: string
          maxLength: 40
          description: Optional marketing tier label (e.g. Premium)
          examples: [Premium, "Ultra Premium"]

    CreditCardWrite:
      allOf:
        - $ref: "#/components/schemas/CreditCard"
      description: Full card document for create/replace

    CreditCardPatch:
      type: object
      additionalProperties: false
      minProperties: 1
      description: Partial update; `id` is immutable and must not be sent
      properties:
        issuer:
          $ref: "#/components/schemas/Issuer"
        name:
          type: string
          minLength: 1
          maxLength: 120
        annualFee:
          type: number
          minimum: 0
        pointCurrency:
          $ref: "#/components/schemas/PointCurrency"
        rewardCategories:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/RewardCategory"
        lastVerified:
          type: string
          format: date
        welcomeOffer:
          oneOf:
            - $ref: "#/components/schemas/WelcomeOffer"
            - type: "null"
        network:
          oneOf:
            - $ref: "#/components/schemas/Network"
            - type: "null"
        tier:
          oneOf:
            - type: string
              maxLength: 40
            - type: "null"

    CardListResponse:
      type: object
      required: [data, meta]
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/CreditCard"
        meta:
          type: object
          required: [total, limit, offset]
          additionalProperties: false
          properties:
            total:
              type: integer
              minimum: 0
            limit:
              type: integer
              minimum: 1
            offset:
              type: integer
              minimum: 0

    SpendToDate:
      type: object
      additionalProperties: false
      description: >
        Spend already applied toward caps (CAD). Not persisted by the engine;
        client or future spend-tracking service supplies it.
      properties:
        monthly:
          type: object
          additionalProperties:
            type: number
            minimum: 0
          description: Map of Category → CAD spent this month toward caps
        annual:
          type: object
          additionalProperties:
            type: number
            minimum: 0
          description: Map of Category → CAD spent this year toward caps

    RecommendationRequest:
      type: object
      required: [amountCad, category]
      additionalProperties: false
      properties:
        amountCad:
          type: number
          exclusiveMinimum: 0
          description: Purchase amount in CAD
        category:
          $ref: "#/components/schemas/Category"
        merchant:
          type: string
          maxLength: 200
          description: >
            Free-text merchant / store name for display.
        merchantBrandId:
          type: string
          description: >
            When set, ranking uses partnership-aware
            `recommendCardsForMerchant()` for this catalog brand id.
          examples: [shell, costco-gas]
        ownedCardIds:
          type: array
          minItems: 1
          uniqueItems: true
          items:
            $ref: "#/components/schemas/CardId"
          description: >
            Explicit wallet. Required when unauthenticated. When authenticated,
            omit to use `user_cards` for the caller; if both are present,
            the body list wins.
        spendToDate:
          $ref: "#/components/schemas/SpendToDate"
        valuations:
          type: object
          additionalProperties:
            type: number
            exclusiveMinimum: 0
          description: >
            Optional overrides for cents CAD per point, keyed by PointCurrency.
            Unspecified currencies use defaults from GET /valuations.
        limit:
          type: integer
          minimum: 1
          maximum: 50
          default: 10
          description: Max recommendations to return (after ranking)

    RecommendationItem:
      type: object
      required:
        - rank
        - card
        - earnRate
        - pointValue
        - centsPerDollar
        - estimatedCentsBack
        - estimatedRewardCad
        - capExhausted
        - reason
      additionalProperties: false
      description: >
        One ranked result. Core fields mirror `Recommendation` in
        `@northtap/core`; purchase-scaled estimates are API additions.
      properties:
        rank:
          type: integer
          minimum: 1
          description: 1-based position after sort
        card:
          $ref: "#/components/schemas/CreditCard"
        earnRate:
          type: number
          minimum: 0
          description: Effective earn rate after caps (points or cashback % per $)
        pointValue:
          type: number
          minimum: 0
          description: Cents CAD per point (1 for cashback)
        centsPerDollar:
          type: number
          minimum: 0
          description: earnRate × pointValue
        estimatedCentsBack:
          type: number
          minimum: 0
          description: amountCad × centsPerDollar
        estimatedRewardCad:
          type: number
          minimum: 0
          description: estimatedCentsBack / 100
        capExhausted:
          type: boolean
          description: True when a spend cap forced fallback to base/`other` rate
        reason:
          type: string
          minLength: 1
          description: Short human-readable explanation of the earn math
        usedPartnership:
          type: boolean
          description: True when a merchant partnership drove the score/reason
        partnershipId:
          type: string
          description: Partnership id when usedPartnership is true

    RecommendationResponse:
      type: object
      required: [purchase, recommendations]
      additionalProperties: false
      properties:
        purchase:
          type: object
          required: [amountCad, category]
          additionalProperties: false
          properties:
            amountCad:
              type: number
            category:
              $ref: "#/components/schemas/Category"
            merchant:
              type: [string, "null"]
            merchantBrandId:
              type: [string, "null"]
        recommendations:
          type: array
          items:
            $ref: "#/components/schemas/RecommendationItem"
        bestCardId:
          type: [string, "null"]
          description: Id of rank-1 card, or null if the wallet yielded no results

    CategoryListResponse:
      type: object
      required: [categories]
      additionalProperties: false
      properties:
        categories:
          type: array
          items:
            type: object
            required: [id, label]
            additionalProperties: false
            properties:
              id:
                $ref: "#/components/schemas/Category"
              label:
                type: string
                description: UI label (CATEGORY_LABELS)

    PointValuationsResponse:
      type: object
      required: [valuations, unit]
      additionalProperties: false
      properties:
        unit:
          type: string
          const: cents_cad_per_point
        valuations:
          type: object
          additionalProperties:
            type: number
            exclusiveMinimum: 0
          description: Keyed by PointCurrency
          example:
            Aeroplan: 2.0
            "Scene+": 1.0
            "TD Rewards": 0.5
            "RBC Avion": 1.7
            "CIBC Aventura": 1.2
            "Amex MR": 2.4
            "BMO Rewards": 0.7
            WestJet: 1.0
            cashback: 1.0
            "PC Optimum": 1.0
            Rogers: 1.5
            Triangle: 1.0

    UserCard:
      type: object
      required: [id, cardId, addedAt]
      additionalProperties: false
      description: Row shape for `public.user_cards` (camelCase in this API)
      properties:
        id:
          type: string
          format: uuid
          description: Corresponds to user_cards.id
        cardId:
          $ref: "#/components/schemas/CardId"
        addedAt:
          type: string
          format: date-time
          description: Corresponds to user_cards.added_at

    UserCardListResponse:
      type: object
      required: [data]
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/UserCard"


    CatalogStatus:
      type: string
      enum: [pending, verified, stale, rejected]

    LoyaltyProgram:
      type: object
      required: [id, name, sourceUrl, lastVerified]
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: [string, "null"]
        pointCurrency:
          oneOf:
            - $ref: "#/components/schemas/PointCurrency"
            - type: "null"
        sourceUrl:
          type: string
          format: uri
        lastVerified:
          type: string
          format: date

    LoyaltyProgramListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/LoyaltyProgram"

    MerchantBrand:
      type: object
      required: [id, name, category, sourceUrl, lastVerified]
      properties:
        id:
          type: string
        name:
          type: string
        category:
          $ref: "#/components/schemas/Category"
        operator:
          type: [string, "null"]
        notes:
          type: [string, "null"]
        sourceUrl:
          type: string
          format: uri
        lastVerified:
          type: string
          format: date

    MerchantBrandListResponse:
      type: object
      required: [data, meta]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/MerchantBrand"
        meta:
          type: object
          required: [total, limit, offset]
          properties:
            total:
              type: integer
            limit:
              type: integer
            offset:
              type: integer

    PartnershipBenefit:
      type: object
      required: [kind, amount, summary]
      properties:
        kind:
          type: string
          enum:
            - cents_per_litre_instant
            - cents_per_litre_rewards
            - points_per_litre
            - points_per_dollar
            - cashback_percent
        amount:
          type: number
        summary:
          type: string
        appliesTo:
          type: string
        capLitresMonthly:
          type: number
        capLitresPerFill:
          type: number
        capAnnualSpendCad:
          type: number
        promotional:
          type: boolean
        promotionalEnds:
          type: string
          format: date
        pointCurrency:
          $ref: "#/components/schemas/PointCurrency"

    MerchantPartnership:
      type: object
      required:
        - id
        - brandIds
        - cardIds
        - affiliation
        - requirements
        - benefits
        - stacksWithCardCategoryRewards
        - sourceUrls
        - lastVerified
      properties:
        id:
          type: string
        brandIds:
          type: array
          items:
            type: string
        merchantBrandIds:
          type: array
          items:
            type: string
          description: Alias of brandIds (packages/core field name)
        loyaltyProgramId:
          type: [string, "null"]
        cardIds:
          type: array
          items:
            $ref: "#/components/schemas/CardId"
        affiliation:
          type: string
          enum: [linked, affiliated, direct]
        requirements:
          type: string
        benefits:
          type: array
          items:
            $ref: "#/components/schemas/PartnershipBenefit"
        stacksWithCardCategoryRewards:
          type: boolean
        notes:
          type: [string, "null"]
        sourceUrls:
          type: array
          items:
            type: string
            format: uri
        sourceUrl:
          type: [string, "null"]
          description: Convenience — first entry of sourceUrls
        lastVerified:
          type: string
          format: date
        status:
          $ref: "#/components/schemas/CatalogStatus"

    PartnershipListResponse:
      type: object
      required: [data, meta]
      properties:
        brandId:
          type: string
        data:
          type: array
          items:
            $ref: "#/components/schemas/MerchantPartnership"
        meta:
          type: object
          required: [total, limit, offset]
          properties:
            total:
              type: integer
            limit:
              type: integer
            offset:
              type: integer

    Error:
      type: object
      required: [error]
      additionalProperties: false
      properties:
        error:
          type: object
          required: [code, message]
          additionalProperties: false
          properties:
            code:
              type: string
              examples:
                - bad_request
                - unauthorized
                - forbidden
                - not_found
                - conflict
                - empty_wallet
            message:
              type: string
            details:
              type: object
              additionalProperties: true
